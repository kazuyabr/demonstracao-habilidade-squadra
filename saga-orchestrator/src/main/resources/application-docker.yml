spring:
  config:
    activate:
      on-profile: docker

  datasource:
    url: jdbc:sqlserver://sqlserver:1433;databaseName=saga_db;encrypt=true;trustServerCertificate=true

pulsar:
  service-url: pulsar://pulsar:6650
